server_id = "osv_mcp"
display_name = "OSV MCP Advisory"
version = "1.2.0"
domain = "security"
sdk_version = "0.1.0"
risk_level = "low"
writes_allowed = "none"
transports = ["stdio"]
credentials = []
governance_gates = []
environments = ["development", "staging", "production"]

[[tools]]
name = "search_advisories"
description = "Search advisories (CVE/GHSA/OSV/RustSec) by package, ecosystem, or keyword"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "get_advisory"
description = "Get full advisory details by ID (CVE-*, GHSA-*, RUSTSEC-*, OSV-*)"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "map_vulnerability_to_dependency"
description = "Map advisories against project dependencies from lockfiles"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "rank_security_risk"
description = "Score practical risk - severity, exploitability, exposure, reachability"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "generate_patch_plan"
description = "Generate a remediation plan: upgrade, patch, workaround, rollout order"
risk_class = "read_only"
requires_approval = false
credential_bindings = []

[[tools]]
name = "export_security_evidence"
description = "Export security findings as audit/compliance evidence"
risk_class = "read_only"
requires_approval = false
credential_bindings = []